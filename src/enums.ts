export enum ViewMode {
  SIDE_BY_SIDE = "SIDE_BY_SIDE",
  CONTENT = "CONTENT",
  PREVIEW = "PREVIEW",
}

export enum GridArea {
  SIDEBAR = "SIDEBAR",
  LEFT = "LEFT",
  MAIN = "MAIN",
  RIGHT = "RIGHT",
}
